import React from "react"

const PostFormat = ({
    profilePic,
    userid,
    postImages,
    likeCount,
    caption,
    commentCount,
    handleLike,
    isLiked,
    viewLikeList,
    goToProfile,
    viewComments
}) => {
    return (
        <div>

        </div>
    )
}

export default PostFormat